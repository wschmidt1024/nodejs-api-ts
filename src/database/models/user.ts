export interface User {
    id: string;
    first: string;
    last: string;
}

export interface CreateUser {
    first: string;
    last: string;
}
export interface UpdateUser {
    first: string;
    last: string;
}
